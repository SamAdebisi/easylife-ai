apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: easylife-ai-prod

resources:
  - ../../base
  - hpa.yaml
  - pdb.yaml
  - resource-limits.yaml
  - monitoring.yaml

patchesStrategicMerge:
  - production-patches.yaml

commonLabels:
  environment: production
  app.kubernetes.io/part-of: easylife-ai
